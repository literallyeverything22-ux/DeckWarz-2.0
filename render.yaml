services:
  - type: web
    name: deckwarz
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -k eventlet -w 1 main:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
      - key: DECKWARZ_SECRET_KEY
        generateValue: true
